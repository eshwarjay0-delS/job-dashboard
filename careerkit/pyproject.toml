[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",   # line too long (handled by formatter)
    "F841",   # local var assigned but never used (intentional try/except assignments)
]
exclude = [".venv", "uploads", "outputs"]
